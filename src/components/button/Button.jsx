import React from 'react'

const Button = ({text}) => {
  return (
    <button className=' bg-primary py-[25px]  px-[72px] uppercase text-base text-white font-bold  rounded-full'>
        {text}
      
    </button>
  )
}

export default Button
